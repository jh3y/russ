{
  "name": "bolt",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:stylus": "node ./tasks/watch -d src/stylus -e style -n Stylus",
    "compile": "node ./tasks/compile",
    "watch": "npm run watch:stylus -s",
    "test": "echo \"Error: no test specified\" && exit 1",
    "native": "stylus src/stylus/style.stylus --out public",
    "build:styles": "node ./tasks/style"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "async": "^1.5.0",
    "autoprefixer": "^6.0.3",
    "commander": "^2.9.0",
    "glob": "^5.0.15",
    "postcss": "^5.0.10",
    "stylus": "^0.52.4"
  }
}
